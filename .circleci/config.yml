version: 2
jobs:
  build:
    machine:
    steps:
      - checkout
      - run:
          name: Run unit tests
          command: cd sample-android && bundle exec fastlane android unit_tests


version: 2
jobs:
  build:
    macos:
      xcode: "9.2.0"
    shell: /bin/bash --login -eo pipefail
    steps:
      - checkout
      - run:
          name: Run unit tests
          command: cd sample-android && bundle && bundle exec fastlane android unit_tests
