version: 2
jobs:
 build:
   machine: true
   steps:
     - checkout
     - run:
          name: Set Ruby Version
          command:  echo "ruby-2.4" > ~/.ruby-version
     - run:
          name: Bundler install
          command: bundle install   
     - run: cd sample-android && bundle exec fastlane android unit_tests
